image: registry.gitlab.ics.muni.cz:443/europdx/ci-images/docker-extended:latest

services:
  - docker:dind

before_script:
#  - apk add --update python py-pip python-dev && pip install docker-compose # install docker-compose
  - docker version
  - docker-compose version

build-proxy:
  stage: build
  script:
    - docker login -u "$CI_REGISTRY_USER" -p "$CI_REGISTRY_PASSWORD" $CI_REGISTRY
    - cd proxy
    - docker build --pull -t "$CI_REGISTRY_IMAGE/proxy:$CI_COMMIT_REF_SLUG-$CI_COMMIT_SHA" -t "$CI_REGISTRY_IMAGE/proxy:$CI_COMMIT_REF_SLUG" .
    - docker push "$CI_REGISTRY_IMAGE/proxy:$CI_COMMIT_REF_SLUG-$CI_COMMIT_SHA"
    - docker push "$CI_REGISTRY_IMAGE/proxy:$CI_COMMIT_REF_SLUG"
  only:
    - master
    - beta
    - dev

deploy-init-master:
  stage: deploy
  script:
    - eval $(ssh-agent -s)
    - echo "$SSH_PRIVATE_KEY" | tr -d '\r' | ssh-add - > /dev/null
    - ssh -o StrictHostKeyChecking=no $DEPLOY_TARGET "git clone https://gitlab.ics.muni.cz/europdx/dataportal/dataportal-docker.git; mkdir credentials && cd credentials && git clone https://$DEPLOY_TOKEN:$DEPLOY_TOKEN_PASSWORD@gitlab.ics.muni.cz/europdx/credentials/dataportal.git; cd ~; ./init_script.sh $CI_COMMIT_REF_SLUG;"
  environment:
    name: master
    url: $DEPLOY_TARGET
  only:
    - master
  when: manual
  allow_failure: false

deploy-init-beta:
  stage: deploy
  script:
    - eval $(ssh-agent -s)
    - echo "$SSH_PRIVATE_KEY" | tr -d '\r' | ssh-add - > /dev/null
    - ssh -o StrictHostKeyChecking=no $DEPLOY_TARGET_BETA "git clone https://gitlab.ics.muni.cz/europdx/dataportal/dataportal-docker.git; mkdir credentials && cd credentials && git clone https://$DEPLOY_TOKEN:$DEPLOY_TOKEN_PASSWORD@gitlab.ics.muni.cz/europdx/credentials/dataportal.git; cd ~; ./init_script.sh $CI_COMMIT_REF_SLUG;"
  environment:
    name: master
    url: $DEPLOY_TARGET_BETA
  only:
    - master
  when: manual
  allow_failure: false

deploy-init-dev:
  stage: deploy
  script:
    - eval $(ssh-agent -s)
    - echo "$SSH_PRIVATE_KEY" | tr -d '\r' | ssh-add - > /dev/null
    - ssh -o StrictHostKeyChecking=no $DEPLOY_TARGET_DEV "git clone https://gitlab.ics.muni.cz/europdx/dataportal/dataportal-docker.git; mkdir credentials && cd credentials && git clone https://$DEPLOY_TOKEN:$DEPLOY_TOKEN_PASSWORD@gitlab.ics.muni.cz/europdx/credentials/dataportal.git; cd ~; ./init_script.sh $CI_COMMIT_REF_SLUG;"
  environment:
    name: master
    url: $DEPLOY_TARGET_DEV
  only:
    - master
  when: manual
  allow_failure: false

deploy-master:
  stage: deploy
  script:
    - eval $(ssh-agent -s)
    - echo "$SSH_PRIVATE_KEY" | tr -d '\r' | ssh-add - > /dev/null
    - ssh -o StrictHostKeyChecking=no $DEPLOY_TARGET "cd dataportal-docker; git pull; docker-compose pull; docker-compose up -d"
  environment:
    name: master
    url: $DEPLOY_TARGET
  only:
    - master

deploy-beta:
  stage: deploy
  script:
    - eval $(ssh-agent -s)
    - echo "$SSH_PRIVATE_KEY" | tr -d '\r' | ssh-add - > /dev/null
    - ssh -o StrictHostKeyChecking=no $DEPLOY_TARGET_BETA "cd dataportal-docker; git pull; docker-compose pull; docker-compose up -d"
  environment:
    name: beta
    url: $DEPLOY_TARGET_BETA
  only:
    - beta

deploy-dev:
  stage: deploy
  script:
    - eval $(ssh-agent -s)
    - echo "$SSH_PRIVATE_KEY" | tr -d '\r' | ssh-add - > /dev/null
    - ssh -o StrictHostKeyChecking=no $DEPLOY_TARGET_DEV "cd dataportal-docker; git pull; docker-compose pull; docker-compose up -d"
  environment:
    name: dev
    url: $DEPLOY_TARGET_DEV
  only:
    - dev


test-build-master:
  stage: test
  script:
    #    - mkdir -p proxy/ssl
    #    - touch proxy/ssl/server.key
    #    - touch proxy/ssl/server.crt
    - docker-compose build
  only:
    - master
